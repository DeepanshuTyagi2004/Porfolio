import { motion } from "framer-motion";
import { fadeIn } from "../utils/motion";
function Hero() {
  return (
    <div className="flex flex-col md:flex-row sm:px-[356px] px-6 sm:py-16 py-10 text-white">
      Hero
    </div>
  )
}
export default Hero